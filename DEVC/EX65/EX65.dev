#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	char str1[] = 'Hello';
	char *str2  = 'Goodbye';
	
	printf('%d %d %s', &str1, str1, str1);
	printf('%d %d %s', &str2, str2, str2);
	
	return 0;
}
